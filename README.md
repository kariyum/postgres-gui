# Pgeru

A native PostgreSQL desktop application with a built-in AI assistant.

## Features

- Connect to any PostgreSQL database
- AI-powered chat to explore your schema, write queries, and understand your data
- Supports multiple LLM providers (Anthropic, OpenCode-compatible APIs)
- Native, custom-styled window UI

## Build

```bash
cargo build --release
```